_inherit: _base.yaml
name: evidential
# PRIMARY METHOD. Deep Evidential Regression: per-dim Normal-Inverse-Gamma,
# gives aleatoric + epistemic uncertainty in a single forward pass.
model:
  head: evidential
train:
  select_by: calibration
  evidential_lambda: 0.01
uncertainty:
  wrapper: none
